import store from '@/store/index'

export default (to, from, next) => {
  // const isUserLoggedIn = store.state.user.isUserLoggedIn
  // if (isUserLoggedIn) {
  //   next(false)
  // } else {
  //   next()
  // }
}