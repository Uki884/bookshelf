{
  "name": "bookshelf",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:front": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "concurrently npm:front npm:backend",
    "backend": "cd backend && npm run start",
    "front": "cd frontend && npm run serve"
  },
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0"
  },
  "devDependencies": {}
}
